```
usage: duplicates [-h] directories [directories ...]

Duplicates finds duplicate files in multiple directories.

positional arguments:
  directories

optional arguments:
  -h, --help   show this help message and exit
```
